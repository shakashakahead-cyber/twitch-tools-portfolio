---
import IchiBrowserSection from "../components/IchiBrowserSection.astro";
import Layout from "../layouts/Layout.astro";
import Navigation from "../components/Navigation.astro";
import ContactSection from "../components/ContactSection.astro";
import UpdatesSection from "../components/UpdatesSection.astro";
import TwitchEmbed from "../components/TwitchEmbed.astro";
import {
    socialLinks,
    extensions,
    desktopApps,
    profile,
} from "../data/siteData";

const currentLang = "ja";
const strings = profile[currentLang];
const toolList = extensions[currentLang];
const desktopAppList = desktopApps[currentLang];
---

<Layout
    title="暁左衛門 Lab | 配信者向けツール"
    description="配信の悩みを解決するツールをまとめた個人サイトです。"
>
    <!-- Navigation -->
    <Navigation />

    <!-- Hero Section -->
    <main class="relative isolate overflow-hidden pt-14">
        <div
            class="absolute inset-x-0 -top-40 -z-10 transform-gpu overflow-hidden blur-3xl sm:-top-80"
            aria-hidden="true"
        >
            <div
                class="relative left-[calc(50%-11rem)] aspect-[1155/678] w-[36.125rem] -translate-x-1/2 rotate-[30deg] bg-gradient-to-tr from-[#ff80b5] to-[#9089fc] opacity-20 sm:left-[calc(50%-30rem)] sm:w-[72.1875rem]"
                style="clip-path: polygon(74.1% 44.1%, 100% 61.6%, 97.5% 26.9%, 85.5% 0.1%, 80.7% 2%, 72.5% 32.5%, 60.2% 62.4%, 52.4% 68.1%, 47.5% 58.3%, 45.2% 34.5%, 27.5% 76.7%, 0.1% 64.9%, 17.9% 100%, 27.6% 76.8%, 76.1% 97.7%, 74.1% 44.1%)"
            >
            </div>
        </div>

        <div class="mx-auto max-w-7xl px-6 py-24 sm:py-32 lg:px-8 text-center">
            <div class="mb-8 flex justify-center">
                <div
                    class="relative rounded-full px-3 py-1 text-sm leading-6 text-gray-400 ring-1 ring-white/10 hover:ring-white/20"
                >
                    {strings.role}
                </div>
            </div>
            <h1
                class="text-4xl font-bold tracking-tight text-white sm:text-6xl mb-6"
            >
                暁左衛門 Lab
            </h1>
            <p class="mt-6 text-lg leading-8 text-gray-300 max-w-2xl mx-auto">
                配信で感じた不便を解決するために、Twitch向けのツールを作っています。<br />
                まずはツール一覧から、気になるものを試してみてください。
            </p>

            <div
                class="mt-10 flex flex-col sm:flex-row items-center justify-center gap-6"
            >
                <a
                    href="/tools/"
                    class="rounded-md bg-indigo-600 px-3.5 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-indigo-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600 transition-colors"
                >
                    ツール一覧を見る
                </a>
                <a
                    href="/blog/"
                    class="text-sm font-semibold leading-6 text-white hover:text-indigo-300 transition-colors"
                >
                    ブログを読む <span aria-hidden="true">&rarr;</span>
                </a>
            </div>
        </div>
    </main>

    <!-- Tools Section (Digest) -->
    <section class="bg-slate-800/50 py-16 relative z-10">
        <div class="mx-auto max-w-7xl px-6 lg:px-8">
            <div class="text-center mb-10">
                <h2 class="text-2xl font-bold tracking-tight text-white mb-4">
                    おすすめツール
                </h2>
                <a
                    href="/tools/"
                    class="text-indigo-400 hover:text-indigo-300 text-sm font-semibold"
                >
                    ツール一覧を見る <span aria-hidden="true">&rarr;</span>
                </a>
            </div>

            <!-- Tools section digest -->
            <div
                class="mx-auto grid max-w-2xl grid-cols-1 gap-x-8 gap-y-12 lg:mx-0 lg:max-w-none lg:grid-cols-2"
            >
                {
                    toolList.map((ext) => (
                        // Tool card digest
                        <div class="group relative flex flex-col overflow-hidden rounded-2xl bg-slate-900 shadow-xl ring-1 ring-white/10 transition-all hover:ring-indigo-500 hover:-translate-y-1">
                            <a
                                href={ext.link}
                                class="relative h-80 overflow-hidden bg-slate-800 block"
                            >
                                <img
                                    src={ext.image}
                                    alt={ext.title}
                                    class="absolute inset-0 h-full w-full object-cover opacity-80 group-hover:opacity-100 transition-opacity"
                                />
                                <div class="absolute inset-0 bg-gradient-to-t from-slate-900 via-transparent" />
                            </a>
                            <div class="p-6 flex flex-col flex-1">
                                <a
                                    href={ext.link}
                                    class="flex items-center gap-x-3 text-lg font-bold text-white mb-2 hover:text-indigo-400"
                                >
                                    <span>{ext.icon}</span>
                                    {ext.title}
                                </a>
                                {ext.badge && (
                                    <div class="mb-2">
                                        <span class="inline-block bg-indigo-500 text-white text-[10px] font-bold px-2 py-0.5 rounded uppercase tracking-wider">
                                            {ext.badge}
                                        </span>
                                    </div>
                                )}
                                <p class="text-sm text-gray-400 line-clamp-2 mb-4">
                                    {ext.desc}
                                </p>

                                {ext.features && (
                                    <ul class="mb-4 space-y-1">
                                        {ext.features
                                            .slice(0, 2)
                                            .map((feature) => (
                                                <li class="text-xs text-gray-400 flex items-start gap-2">
                                                    <span class="text-indigo-500 mt-0.5">
                                                        •
                                                    </span>
                                                    {feature}
                                                </li>
                                            ))}
                                    </ul>
                                )}

                                <div class="mt-auto pt-4 border-t border-white/5 flex items-center justify-between gap-4">
                                    <a
                                        href={ext.link}
                                        class="text-sm font-semibold text-indigo-400 hover:text-indigo-300 flex items-center gap-1"
                                    >
                                        詳細を見る{" "}
                                        <span aria-hidden="true">&rarr;</span>
                                    </a>
                                    <div class="flex items-center gap-2">
                                        {ext.repoUrl && (
                                            <a
                                                href={ext.repoUrl}
                                                target="_blank"
                                                class="text-xs font-semibold text-gray-300 hover:text-white transition-colors"
                                            >
                                                GitHub
                                            </a>
                                        )}
                                        <a
                                            href={ext.storeUrl}
                                            target="_blank"
                                            class="rounded-md bg-white/10 px-3 py-2 text-xs font-semibold text-white shadow-sm hover:bg-white/20 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-500 transition-colors flex items-center gap-2"
                                        >
                                            <svg
                                                viewBox="0 0 24 24"
                                                fill="currentColor"
                                                class="w-4 h-4 text-indigo-400"
                                            >
                                                <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-1 17.93c-3.95-.49-7-3.85-7-7.93 0-.62.08-1.21.21-1.79L9 15v1c0 1.1.9 2 2 2v1.93zm6.9-2.54c-.26-.81-1-1.39-1.9-1.39h-1v-3c0-.55-.45-1-1-1H8v-2h2c.55 0 1-.45 1-1V7h2c1.1 0 2-.9 2-2v-.41c2.93 1.19 5 4.06 5 7.41 0 2.08-.8 3.97-2.1 5.39z" />
                                            </svg>
                                            Chromeに追加
                                        </a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    ))
                }
            </div>
        </div>
    </section>

    <IchiBrowserSection />

    <!-- Desktop Apps Section -->
    {
        desktopAppList.length > 0 && (
            <section class="bg-slate-900 py-16 relative z-10">
                <div class="mx-auto max-w-7xl px-6 lg:px-8">
                    <div class="text-center mb-10">
                        <h2 class="text-2xl font-bold tracking-tight text-white mb-4">
                            Desktop Applications
                        </h2>
                        <p class="text-gray-400 text-sm">
                            Windows向けデスクトップアプリ
                        </p>
                    </div>

                    <div class="mx-auto max-w-2xl">
                        {desktopAppList.map((app) => (
                            <div class="group relative flex flex-col overflow-hidden rounded-2xl bg-slate-800 shadow-xl ring-1 ring-white/10 transition-all hover:ring-purple-500 hover:-translate-y-1">
                                <a
                                    href={app.link}
                                    class="relative h-64 overflow-hidden bg-slate-700 block"
                                >
                                    <img
                                        src={app.image}
                                        alt={app.title}
                                        class="absolute inset-0 h-full w-full object-cover opacity-80 group-hover:opacity-100 transition-opacity"
                                    />
                                    <div class="absolute inset-0 bg-gradient-to-t from-slate-900 via-transparent" />
                                    <div class="absolute top-4 right-4 flex items-center gap-2">
                                        <span class="inline-flex items-center gap-1 bg-slate-900/80 text-gray-300 text-xs px-2 py-1 rounded-full backdrop-blur-sm">
                                            <svg
                                                xmlns="http://www.w3.org/2000/svg"
                                                class="w-3 h-3"
                                                viewBox="0 0 24 24"
                                                fill="currentColor"
                                            >
                                                <path d="M0 3.449L9.75 2.1v9.451H0m10.949-9.602L24 0v11.4H10.949M0 12.6h9.75v9.451L0 20.699M10.949 12.6H24V24l-12.9-1.801" />
                                            </svg>
                                            {app.platform}
                                        </span>
                                    </div>
                                </a>
                                <div class="p-6 flex flex-col flex-1">
                                    <a
                                        href={app.link}
                                        class="flex items-center gap-x-3 text-lg font-bold text-white mb-2 hover:text-purple-400"
                                    >
                                        <span>{app.icon}</span>
                                        {app.title}
                                    </a>
                                    {app.badge && (
                                        <div class="mb-2">
                                            <span class="inline-block bg-purple-500 text-white text-[10px] font-bold px-2 py-0.5 rounded uppercase tracking-wider">
                                                {app.badge}
                                            </span>
                                        </div>
                                    )}
                                    <p class="text-sm text-gray-400 line-clamp-2 mb-4">
                                        {app.desc}
                                    </p>
                                    {app.features && (
                                        <ul class="mb-4 space-y-1">
                                            {app.features
                                                .slice(0, 2)
                                                .map((feature) => (
                                                    <li class="text-xs text-gray-400 flex items-start gap-2">
                                                        <span class="text-purple-500 mt-0.5">
                                                            •
                                                        </span>
                                                        {feature}
                                                    </li>
                                                ))}
                                        </ul>
                                    )}
                                    <div class="mt-auto pt-4 border-t border-white/5 flex items-center justify-between gap-4">
                                        <a
                                            href={app.link}
                                            class="text-sm font-semibold text-purple-400 hover:text-purple-300 flex items-center gap-1"
                                        >
                                            詳細を見る{" "}
                                            <span aria-hidden="true">
                                                &rarr;
                                            </span>
                                        </a>
                                        <div class="flex items-center gap-2">
                                            <span class="text-xs text-gray-500">
                                                {app.fileSize}
                                            </span>
                                            <a
                                                href={app.downloadUrl}
                                                target="_blank"
                                                class="rounded-md bg-gradient-to-r from-purple-600 to-indigo-600 px-3 py-2 text-xs font-semibold text-white shadow-sm hover:from-purple-500 hover:to-indigo-500 transition-colors flex items-center gap-2"
                                            >
                                                <svg
                                                    xmlns="http://www.w3.org/2000/svg"
                                                    fill="none"
                                                    viewBox="0 0 24 24"
                                                    stroke-width="2"
                                                    stroke="currentColor"
                                                    class="w-4 h-4"
                                                >
                                                    <path
                                                        stroke-linecap="round"
                                                        stroke-linejoin="round"
                                                        d="M3 16.5v2.25A2.25 2.25 0 0 0 5.25 21h13.5A2.25 2.25 0 0 0 21 18.75V16.5M16.5 12 12 16.5m0 0L7.5 12m4.5 4.5V3"
                                                    />
                                                </svg>
                                                ダウンロード
                                            </a>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        ))}
                    </div>
                </div>
            </section>
        )
    }

    <!-- About Section (Digest) -->
    <section class="bg-slate-800 relative isolate overflow-hidden py-16">
        <div
            class="mx-auto max-w-7xl px-6 lg:px-8 lg:flex lg:items-center lg:justify-between lg:gap-16"
        >
            <div class="max-w-xl">
                <h2
                    class="text-2xl font-bold tracking-tight text-white sm:text-3xl mb-4"
                >
                    About Developer
                </h2>
                <p class="text-gray-300 mb-6">
                    {strings.description}
                </p>
                <div class="flex items-center gap-4">
                    <a
                        href="/about/"
                        class="text-sm font-semibold text-indigo-400 hover:text-indigo-300"
                    >
                        元自衛官の経歴や、お仕事の依頼はこちら <span aria-hidden="true">&rarr;</span>
                    </a>
                </div>
            </div>
            <!-- Simple about section with Twitch embed -->
            <div class="mt-10 lg:mt-0 w-full max-w-md lg:shrink-0">
                <TwitchEmbed channel="akatsukizaemon" />
            </div>
        </div>
    </section>

    <!-- Updates Section -->
    <UpdatesSection />

    <!-- Contact Section -->
    <ContactSection socialLinks={socialLinks} />

</Layout>
