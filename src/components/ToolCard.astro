---
interface Props {
    title: string;
    desc: string;
    link: string;
    storeUrl: string;
    icon: string;
    image: string;
}

const { title, desc, link, storeUrl, icon, image } = Astro.props;
---

<div
    class="group relative flex flex-col overflow-hidden rounded-2xl bg-slate-900 shadow-xl ring-1 ring-white/10 transition-all hover:ring-indigo-500 hover:-translate-y-1"
>
    <a href={link} class="relative h-48 overflow-hidden bg-slate-800 block">
        <img
            src={image}
            alt={title}
            class="absolute inset-0 h-full w-full object-cover transition-transform duration-500 group-hover:scale-105 opacity-80 group-hover:opacity-100"
        />
        <div
            class="absolute inset-0 bg-gradient-to-t from-slate-900 via-transparent"
        >
        </div>
    </a>

    <div class="flex flex-1 flex-col justify-between p-6">
        <div>
            <a
                href={link}
                class="flex items-center gap-x-3 text-2xl mb-2 hover:text-indigo-400 transition-colors w-fit"
            >
                <span>{icon}</span>
                <h3 class="font-bold text-white">
                    {title}
                </h3>
            </a>
            <p class="text-sm leading-6 text-gray-400">
                {desc}
            </p>
        </div>

        <div
            class="mt-6 flex items-center justify-between gap-4 pt-4 border-t border-white/5"
        >
            <a
                href={link}
                class="text-sm font-semibold text-indigo-400 hover:text-indigo-300 flex items-center gap-1 transition-colors"
            >
                詳細・使い方{" "}
                <span aria-hidden="true">&rarr;</span>
            </a>
            <a
                href={storeUrl}
                target="_blank"
                class="rounded-md bg-white/10 px-3 py-2 text-xs font-semibold text-white shadow-sm hover:bg-white/20 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-500 transition-colors flex items-center gap-2"
            >
                <svg
                    viewBox="0 0 24 24"
                    fill="currentColor"
                    class="w-4 h-4 text-indigo-400"
                >
                    <path
                        d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-1 17.93c-3.95-.49-7-3.85-7-7.93 0-.62.08-1.21.21-1.79L9 15v1c0 1.1.9 2 2 2v1.93zm6.9-2.54c-.26-.81-1-1.39-1.9-1.39h-1v-3c0-.55-.45-1-1-1H8v-2h2c.55 0 1-.45 1-1V7h2c1.1 0 2-.9 2-2v-.41c2.93 1.19 5 4.06 5 7.41 0 2.08-.8 3.97-2.1 5.39z"
                    ></path>
                </svg>
                Storeへ
            </a>
        </div>
    </div>
</div>
